export interface Table {
    _id: string,
    name: string,
    x: number,
    y: number,
    level: number,
}

export interface NewTable {
    name: string,
    x: number,
    y: number,
    level: number,
}